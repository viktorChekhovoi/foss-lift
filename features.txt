FOSSLIFT — FEATURE BACKLOG
==========================

This file is no longer the backlog. Features now live in GitHub Issues:

    https://github.com/viktorChekhovoi/foss-lift/issues

    gh issue list --label p1        # next up
    gh issue list --label p2        # soon
    gh issue list --label p3        # later
    gh issue list --state all --label shipped

Every item that was in this file has been migrated; the original numbering is
preserved as a "Backlog ID:" line at the top of each issue body. Issues #1–#5
are the shipped "builder" milestone. Item 5.7 ("make sure the UI is still
intuitive") was folded into #8 as an acceptance criterion rather than becoming a
standalone issue.

Out-of-scope decisions moved to CLAUDE.md.
